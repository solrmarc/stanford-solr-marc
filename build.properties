# TOP LEVEL solrmarc properties file
java.compat.version = 1.5
name = solrmarc
src.dir = src
lib.dir = lib
build.dir = bin
dist.dir = dist

# is trans.map.path needed?
trans.map.path = ${lib.dir}/solrmarc/translation_maps
script.template.dir = script_templates

# TEST properties
test.dir = test
test.src.dir = ${test.dir}/src
test.build.dir = ${test.dir}/bin
